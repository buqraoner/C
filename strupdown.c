#include<stdio.h>
#include<string.h>
int main( )
{
   /* char a[20]="ANKARA";
    printf("\n");
    printf("Buyuk harfle yazilan  cumle : %s\n", a );
    printf("Kucuk harfedonusturulen cumle: %s\n", strlwr(a) );
    return 0;

    */
//Stringin
//büyük
//harfleri
//küçük harfe
//çevirir
   //  strcmp(a,b);    = iki stringi karşılaştırma  Birinci stringi
/* İkinci string ile karşılaştırır.
Eşitse 0, string1 büyükse 0'dan büyük bir değer 
aksi halde 0'dan küçük bir değer gönderir
   
   #include<stdio.h>
#include<string.h>
int main()
{
char a[10]="denemeabc";
char b[10];
int sonuc; 
{
printf("Bir cumle giriniz : ");
gets(b); 
sonuc = strcmp(a,b);
if( sonuc==0 ){ 
puts("İki cumlede ayni ");
printf("
\n");
puts(a);
puts(b);
}
else
puts("İki cumle ayni degil");
printf("
\n");
puts(a);
puts(b);
}
return 0;
}
   
   
   
   
   */
   
   
   

#include<stdio.h>
#include<string.h>
    int main( )
    {
        char
                a[20]="ankara";
        printf("\n");
        printf(
                "Kucuk harfle yazilan
        cumle : %s\n", a );
        printf(
                "Buyuk harfe
        donusturulen cumle:
        %s\n", strupr(a) );
        return 0;
    }

// Buyuk harf
           
           
           
           


}
